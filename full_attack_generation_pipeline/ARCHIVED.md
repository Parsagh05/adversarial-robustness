# Full end-to-end pipeline

This directory contains the original attack-generation/evaluation pipeline,
including its notebooks, splits, thresholds, tests, and generated results. It
is preserved separately from the fixed-perturbation evaluator and the focused
perturbation dataset generator. Its Kaggle notebooks resolve this directory
explicitly and can be used as entry points for the complete benchmark.
